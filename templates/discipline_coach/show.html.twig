{% extends 'base.html.twig' %}

{% block title %}Discipline{% endblock %}

{% block body %}
    <h1>Discipline</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ discipline.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ discipline.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('discipline_coach_index') }}">back to list</a>

    <a href="{{ path('discipline_coach_edit', {'id': discipline.id}) }}">edit</a>

    {{ include('discipline_coach/_delete_form.html.twig') }}
{% endblock %}
